{
    "name": "esgi-api",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm install --prefix server"
    },
    "dependencies": {
        "axios": "^0.26.1",
        "swagger-ui-express": "^4.3.0"
    }
}
